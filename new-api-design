{
    reflectedType: [ Function something ],
    providedType: [ Function something ], // = reflectedType if nothing is provided
    options: {
        camelCase: boolean,

    }
}

@Property(() => providedType, { name: 'truc', camelCase: true })
Property(typeFunction?: () => Function, options?: Partial<PropertyOptions>)
